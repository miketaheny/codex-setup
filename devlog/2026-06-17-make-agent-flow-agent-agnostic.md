# 2026-06-17 - Make Agent-Flow agent agnostic

- Branch/worktree: `unavailable before Git init` / `/Users/taheny/vault/teamt/codex-setup`
- Commit: `pending`
- Goal: Reframe this setup as AF Agent-Flow for Claude, Codex, and other coding agents while preserving Codex skill compatibility.
- Summary:
  - Added canonical `AGENT-FLOW.md` workflow instructions.
  - Replaced root `AGENTS.md` with a Codex-compatible adapter and added `CLAUDE.md` as a Claude adapter.
  - Updated install and bootstrap scripts to use `~/.agent-flow` as the shared home and install tool-specific adapters for Codex and Claude.
  - Added repo templates for `AGENT-FLOW.md`, `AGENTS.md`, and `CLAUDE.md`.
  - Renamed reusable prompt docs from `docs/CODEX-PROMPTS.md` to `docs/AGENT-PROMPTS.md`.
  - Updated README, workflow docs, commander notes, skill wording, audit helper behavior, and project display metadata to use Agent-Flow naming.
- Files changed:
  - `AGENT-FLOW.md` - canonical shared workflow instructions.
  - `AGENTS.md` - Codex-compatible adapter.
  - `CLAUDE.md` - Claude-compatible adapter.
  - `README.md` - agent-neutral overview and install instructions.
  - `docs/WORKFLOW.md` - agent-neutral workflow guide.
  - `docs/AGENT-PROMPTS.md` - agent-neutral prompt examples.
  - `docs/CODEX-PROMPTS.md` - removed Codex-only prompt doc.
  - `scripts/install.sh` - installs shared Agent-Flow files plus Codex and Claude adapters.
  - `scripts/bootstrap-repo.sh` - bootstraps repo-level Agent-Flow and adapter files.
  - `scripts/new-worktree.sh` - removed Codex-only next-step message.
  - `scripts/review-snapshot.sh` - removed Codex-only review message.
  - `templates/repo-AGENT-FLOW.md` - new canonical project template.
  - `templates/repo-AGENTS.md` - Codex adapter template.
  - `templates/repo-CLAUDE.md` - Claude adapter template.
  - `skills/*/SKILL.md` - updated AF skill wording from Codex-specific to agent-neutral where appropriate.
  - `skills/af-reconcile-worktrees/scripts/audit_repo.py` - updated instruction-file audit to inspect Agent-Flow and adapter files.
  - `.vscode/settings.json` - updated window title to Agent-Flow.
  - `.gitignore` - excluded macOS metadata and Python bytecode before Git initialization.
  - `backlog/config.yml` - renamed project to `agent-flow`.
  - `backlog/tasks/task-3 - Make-Agent-Flow-agent-agnostic.md` - tracked task status and validation.
- Decisions:
  - Use `AGENT-FLOW.md` as the canonical instruction file and keep `AGENTS.md`/`CLAUDE.md` as thin adapters to avoid divergent rules.
  - Keep Codex/OpenAI references only for actual compatibility surfaces: Codex skill format, `.codex` install path, `agents/openai.yaml`, and optional Codex plugin setup.
  - Initialize Git on `development` as the final step because the workspace was not a repository at task start.
- Validation:
  - `bash -n scripts/install.sh scripts/bootstrap-repo.sh scripts/new-worktree.sh scripts/review-snapshot.sh scripts/check-branch-safety.sh` - passed.
  - `quick_validate.py` over every `skills/*` directory - passed.
  - `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile skills/af-reconcile-worktrees/scripts/audit_repo.py` - passed; generated bytecode was removed.
  - Temporary Git repo smoke test with `AF_HOME=/Users/taheny/vault/teamt/codex-setup bash scripts/bootstrap-repo.sh` - passed and created `AGENT-FLOW.md`, `AGENTS.md`, `CLAUDE.md`, `devlog/README.md`, and decision template.
  - `rg` checks over active docs, scripts, templates, and skills - no stale Codex-only branding found outside intentional compatibility references.
- Review:
  - Manual scope review completed. No P1/P2 findings found.
- Risks / follow-ups:
  - This package still uses Codex skill folder conventions for `skills/`; other agents can read the Markdown workflows but may need their own native command wrappers.
  - Git was not initialized until the end of this work by user request, so feature-branch isolation was unavailable during the edit.
  - `backlog/` disappeared unexpectedly during implementation; only the current task record was recreated to avoid reconstructing older task history without confirmation.
