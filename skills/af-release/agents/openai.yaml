interface:
  display_name: "AF Release PR"
  short_description: "Prepare protected release PRs"
  default_prompt: "Use $af-release to push development when ready and prepare the correct staging or main release PR."
