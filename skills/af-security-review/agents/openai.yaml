interface:
  display_name: "AF Security Review"
  short_description: "Run deep security-only review"
  default_prompt: "Use $af-security-review to run a deep security-only review for this sensitive Agent-Flow diff."
